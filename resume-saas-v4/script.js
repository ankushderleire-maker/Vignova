const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function run() { await prisma.$executeRawUnsafe('ALTER TABLE plan_configs ADD COLUMN has_linkedin_optimization BOOLEAN NOT NULL DEFAULT false;'); await prisma.$executeRawUnsafe('ALTER TABLE plan_configs ADD COLUMN has_interview_prep BOOLEAN NOT NULL DEFAULT false;'); console.log('Columns added'); } run();
